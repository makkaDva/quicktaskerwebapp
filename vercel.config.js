module.exports = {
    build: {
      command: "prisma generate && next build", // Replace with your desired build command
      output: "dist" // Replace with your output directory (if needed)
    }
  };